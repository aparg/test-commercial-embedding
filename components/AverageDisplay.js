"use client";
import React, { useEffect, useState } from "react";

const AverageDisplay = ({ main_data }) => {
  // const [avg,setAvg] = useState()
  // useEffect(()=>{
  //     main_data.forEach(element => {
  //         element.
  //     });
  // },[])
  return (
    // main_data.
    `AverageDisplay`
  );
};

export default AverageDisplay;
